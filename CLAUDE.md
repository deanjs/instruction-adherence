# CLAUDE.md

## 프로젝트

코딩 에이전트의 지침 준수율 하락에서 **선행 코드 패턴의 인과적 역할**을 검증한다.
건국대학교 ELION LAB.

가설: 에이전트가 `AGENTS.md` / `CLAUDE.md`의 규칙을 세션 후반에 덜 지키는 이유가,
context에 누적된 이전 코드를 규칙보다 우선해서 참조하기 때문일 수 있다.

관찰 연구로는 답할 수 없으므로(첫 위반 이전 준수율은 정의상 항상 100%),
**context를 직접 만들어 무작위 배정하는 통제 실험**으로 접근한다.

전체 설계는 `docs/연구계획서.md`에 있다.
**통째로 읽지 말 것.** 지금 하는 작업에 해당하는 절만 찾아 읽는다.

| 작업 | 읽을 절 |
|---|---|
| 행동 실험 하네스 | 3.3, 6.1, 6.2 |
| 준수 판정기 (AST / git diff) | 3.2, 6.5 |
| 준수 선호 점수 계산 | 3.5 |
| attention 관측 | 3.4, 3.7 |
| attention 개입 | 3.6 |
| 통계 분석 | 6.4 |
| 파일럿 게이트 | 5.5 |
| 모델·디코딩 설정 | 5.1, 5.3 |

아래 "절대 어기면 안 되는 것" 6개는 계획서에서 추출한 요약이다.
대부분의 작업은 그 6개 + 위 표의 해당 절 하나면 충분하다.

## 3단 구조

| 단계 | 내용 | 실패 시 |
|---|---|---|
| 1 | 통제된 context 조작 → 행동 수준 인과 효과 | 논문의 본체 |
| 2 | 길이 정규화 attention 관측 (지침 참조 비율) | 1단계만으로 성립 |
| 3 | attention 개입 (P1a/P2) → 인과 기여 | 1·2단계만으로 성립 |

어느 단계에서 멈춰도 논문이 성립하도록 설계돼 있다. 앞 단계를 건너뛰고
뒤 단계를 먼저 구현하지 말 것.

## 현재 진행 상황

- [x] **Step 0** — 개입용 함수명 pair 토큰 수 일치 수율 측정
  - Qwen2.5-Coder-7B 기준 **316/396 (79.8%)**
  - `def`, `def_in_class`, `with_code` 세 context에서 동일 → prefix 구성과 무관하게 안정적
  - 단일 토큰 분기 125/316 (39.6%) → 보조 지표 적용 가능 비율
  - 결과: `results/matched_pairs.json`, `results/step0_summary.json`
- [ ] **Step 1** — 기저 준수율 smoke test (계획서 5.5 게이트: 55~85%)
- [x] **Step 2** — `AttentionInterface` 조건부 함수 + 512 토큰 eager 대조 검증
  - 하네스: `src/stage2_attention.py`(`--validate`/`--observe`/`--content`/`--content2x2`), `notebooks/stage2_colab.ipynb`
  - 커널 검증 PASS(`--validate`, 1.5B fp32): eager 대조 2632쌍, 최대 절대오차 5.96e-07
  - 관측 100세션(`results/stage2_niar.jsonl`). 2×2로 "위반 상태" 효과 분리·개입 후보층 선정
- [ ] **Step 3** — attention/KV 개입 (P1a·P2), 주 가설 H3. 계획서 3.5/3.6
  - 하네스 구현: `src/stage3_intervention.py`(`--validate`/`--run-a`/`--run-b`/`--summary-only`), `notebooks/stage3_colab.ipynb`
  - P1a=KV group(4) K/V 치환, P2=query head(28) α 배율, 준수 선호 점수=공통 고정 후보 teacher forcing
  - **Colab에서 `--validate` PASS 확인 후** 실측(`--run-a`→`--run-b`) 진행 — 그 전엔 미완료
  - P1b/P3(탐색)는 v1 미구현(주 가설 H3=P1a에 집중)
- [ ] 이후 계획서 7장 일정 참조

## 구조

```
docs/        연구계획서
src/         실험 스크립트
notebooks/   Colab 진입점
results/     실행 결과 — 사전 등록 기록. 절대 삭제·수정하지 말 것
```

## 실행 환경

- 로컬: macOS (Apple Silicon). 개발과 tokenizer 수준 작업만.
- 실험: **Google Colab**. 모델 추론과 attention 개입은 전부 여기서.
- Colab은 세션이 끊긴다. 장시간 작업은 반드시 재개 가능하게 짤 것 (아래 참조).

## 실험 설계상 절대 어기면 안 되는 것

이 항목들은 편의를 위해 완화하지 말 것. 위반하면 결과가 무효가 된다.

1. **준수 선호 점수 값으로 표본을 선별하지 않는다.**
   `기저 준수 선호 점수 변화량`이 양수인 pair만 골라 개입하면 순환 논증 + 평균 회귀 오독이 된다.
   선별 기준은 토큰 수 일치, 함수명 속성, 명세 슬롯 등 준수 선호 점수와 무관한 사전 기준만.

2. **준수 선호 점수 후보 쌍은 모든 조건에서 동일한 문자열을 채점한다.**
   모델이 실제 생성한 이름에서 후보를 뽑으면 조건 간 비교가 성립하지 않는다.
   실제 생성한 이름은 별도로 기록해 행동 수준 준수 판정에만 쓴다.

3. **개입 실험 pair는 토큰 수가 정확히 일치해야 한다.** 1토큰 차이도 허용 안 됨.
   행동 실험의 ±3 토큰 허용 오차를 개입에 적용하지 말 것.

4. **P1a는 KV group 단위(4개), P2/P1b/P3는 query head 단위(28개).**
   Qwen2.5-Coder는 GQA라 K/V를 query head 7개가 공유한다.
   P1a를 query head 단위로 짜면 실제로는 7개를 동시에 건드리면서 1개로 보고하게 된다.

5. **`results/` 아래 사전 등록 파일은 수정하지 않는다.**
   재생성이 필요하면 새 파일명으로 만들고 기존 것은 남긴다.

6. **주 가설은 H1a, H2a, H3 뿐.** 나머지는 탐색적 보고. 사후에 주 가설을 추가하지 않는다.

## 코딩 규약

- Python 3.11+, 표준 라이브러리 우선. 의존성 추가는 `requirements.txt`에 명시.
- 스크립트는 `src/`에, `argparse`로 인자를 받고 결과는 `results/`에 JSON으로 저장.
- 모든 실행에 seed를 기록한다. 대응 설계상 같은 seed가 조건 간에 공유돼야 한다.
- 함수명은 snake_case (이 repo의 규약).
  실험 **대상** 코드의 표기 규칙과 혼동하지 말 것 — 그건 실험 조작이지 이 repo의 스타일이 아니다.
- 주석과 문서는 한국어. 코드 식별자는 영어.

## Colab 제약

- 세션이 끊기므로 **결과는 세션 하나 끝날 때마다 JSONL에 append**한다.
  러너 시작 시 `(seed, task, condition)` 조합으로 완료분을 건너뛴다.
  2,000세션을 한 번에 돌린다는 전제로 짜지 말 것.
- P2 스윕은 층 × head × λ로 config가 수천 개다.
  **prefix KV 캐시를 한 번 만들고 재사용**하는 구조가 필수.
  매번 prefill을 다시 하면 실행 시간이 두 자릿수 배로 늘어난다.
- 개입 실험에 양자화 모델을 쓰지 않는다. activation 수치가 흔들려
  준수 선호 점수 변화량이 개입 효과인지 양자화 노이즈인지 구분되지 않는다.

## 미결정 사항

결정 없이 임의로 채우지 말고 물어볼 것.

- **개입용 모델 크기** — 7B fp16은 무료 Colab T4(16GB)에 안 올라간다.
  1.5B/3B/7B 중 계획서 5.5 게이트(준수율 55~85%)를 통과하는 가장 작은 모델을 쓸 예정.
  Step 1에서 결정한다.
- **언어별 지침 방향** — TypeScript는 camelCase가 원래 관용이라 천장 효과 우려가 있다.
  Python에 camelCase / TS에 snake_case로 뒤집는 안을 검토 중. Step 1 결과로 판단.
- **H2 하네스 세션 수** — 320세션은 Colab에서 30~50시간. 축소 여부 미정.

## 이 프로젝트의 아이러니

이 연구의 주제가 **"`CLAUDE.md`의 규칙이 세션이 길어질수록 안 지켜진다"**는 것이다.
이 파일도 예외가 아니다. 세션 후반에 위 규칙이 무시되는 게 관찰되면
그 자체가 기록할 가치가 있는 관찰이므로 `docs/observations.md`에 남길 것.
