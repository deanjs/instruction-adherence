# 중간 점검 — 코딩 에이전트의 지침 준수율 하락에 대한 선행 코드의 인과적 역할

건국대학교 ELION LAB. / 2026-08-04

---

## 1. 연구 주제와 질문

코딩 에이전트(LLM)는 **세션이 길어질수록 프로젝트 규칙(`CLAUDE.md`·`AGENTS.md`)을 점점 덜 지킨다**는
현상이 보고된다. 우리의 질문은 그 **원인**이다:

> 규칙을 덜 지키게 되는 것이, context에 **누적된 이전 코드 패턴**을 규칙보다 우선해서 참조하기
> 때문인가?

즉 "context가 길어져서"라는 막연한 설명 대신, **선행 코드가 규칙과 경쟁해 준수를 떨어뜨린다**는
구체적 기제를 검증하려는 것이다.

---

## 2. 핵심 접근 — 왜 통제 실험인가

이 질문은 **관찰 연구로는 답할 수 없다.** 실제 세션 로그를 분석하면, 첫 위반이 나오기 전까지의
준수율은 **정의상 항상 100%**라 "무엇이 첫 위반을 유발했는가"를 인과적으로 분리할 수 없다.

그래서 우리는 **context를 직접 만들어 무작위 배정하는 통제 실험**으로 접근한다. 앞부분 코드의
표기 스타일(camelCase 대 snake_case)을 실험자가 조작하고, 규칙은 동일하게 둔 채 뒤에서 만드는
함수가 규칙을 지키는지 본다. 나아가 모델 **내부 계산을 직접 치환**해 인과 기여까지 확인한다.

**실험 설정 요약**
- 실험 모델: **Qwen2.5-Coder-3B-Instruct** (오픈 모델이라 내부 개입 가능; 뒤에 나올 게이트를
  통과하는 가장 작은 모델).
- 조작 대상: 함수명 표기 규칙. 지침은 "모든 함수명을 camelCase로" 처럼 주고, 앞 코드의 표기를
  camel(준수 선례) / snake(위반 선례)로 바꾼다.
- 핵심 측정치 **준수 선호 점수**: 매 순간 모델이 규칙 이름(`formatValue`)과 위반 이름
  (`format_value`) 중 **어느 쪽을 더 선호하는지**를 두 이름의 로그확률 차이로 잰 연속 점수.
  이진 판정(지켰다/어겼다)보다 미세한 변화를 잡는다.

---

## 3. 3단 구조 (설계 철학)

어느 단계에서 멈춰도 논문이 성립하도록 **누적적으로** 설계했다.

| 단계 | 무엇을 보나 | 주장 수준 |
|---|---|---|
| **1단계** | 앞 코드 조작 → 실제 준수 행동이 바뀌나 | **행동 수준 인과** |
| **2단계** | 그때 모델이 규칙 vs 코드를 얼마나 참조하나 (attention) | 상관 관측 |
| **3단계** | 그 attention/내부값을 직접 치환 → 준수 결정이 바뀌나 | **기제 수준 인과** |

---

## 4. 단계별 진행 상황

### 준비 (Step 0) — 개입용 이름쌍 확보 ✅

내부 값을 치환하려면 camel/snake 두 이름이 **토큰 수가 정확히 같아야** 한다(1토큰만 달라도 위치가
어긋나 치환이 정의 안 됨). 함수명 쌍의 토큰 수 일치 수율을 측정 → **316/396 (79.8%)** 확보.

### 1단계 — 행동 수준 인과 (H1a) ✅

**질문:** 앞에 쌓인 위반(snake) 코드가 뒤 함수의 준수율을 실제로 낮추는가?

**방법:** 고정 크기 prefix(함수 12개) 안에서 **남아 있는 준수(camel) 예시 수**를 4→3→2→1→0으로
바꾼다(전체 코드 양은 12로 고정 → "코드가 많아서"가 아니라 "규칙 준수 선례가 사라져서"의 효과를
분리). 규칙 지침은 모든 조건 동일. 세션마다 작업 지시를 위치에 **돌려 배치**해 "몇 번째 자리냐"와
"무슨 작업이냐"의 교란을 제거. **주 결과 = prefix 직후 첫 함수의 준수 여부.**

**결과 (n=20/조건):** 첫 함수 camelCase 준수율

| 남은 준수 예시 | 4 | 3 | 2 | 1 | 0 |
|---|---|---|---|---|---|
| 준수율 | 95% | 85% | 90% | 80% | **30%** |

- **H1a 지지** — 준수 예시 4개(95%) vs 0개(30%), 차이 **65%p**. 앞 코드가 준수를 낮춘다.
- 서서히 주는 게 아니라 **예시가 전부 사라질 때 절벽처럼 급락**(30%).
- **자기증폭 연쇄**: 첫 함수가 규칙을 어기면, 그 이름이 다시 입력에 쌓여 **뒤 함수들도 따라
  무너진다.** 첫 함수가 관문.

### 2단계 — attention 관측 (상관) ✅

**질문:** 위반 상태일 때 모델이 규칙과 앞 코드를 각각 얼마나 **쳐다보나**?

**방법:** 전체 attention을 저장하는 건 불가능(길이 16K에서 층당 수십 GB)하므로, **새로 생성되는
토큰이 context의 어느 구간(지침/준수 코드/위반 코드)을 참조하는지**만 뽑는다. 구간 크기가 달라
생기는 착시를 없애려 **길이 정규화**한다. 1단계와 **완전히 같은 조건·seed**로 100세션 관측.

**결과:** 위반 조건에서 앞 코드 참조가 **중·후반 특정 층에 몰려 증가**(특히 **L25**), 그 값을 근거로
3단계에서 개입할 **후보 층(L25 등)**을 선정. (1차 관측엔 구간 크기 교란이 있어 2차 대응 관측으로
층을 확정.) **이건 상관이지 인과 주장은 아님** — 그래서 3단계로 간다.

### 3단계 — attention/내부값 개입 (H3, 주 가설) ✅

**질문(핵심):** 그 내부 값을 **직접 준수 상태로 갈아끼우면** 준수 결정이 바뀌는가? = 원인인가?

**방법:**
- **P1a(주 개입):** 위반 상태에서 **특정 층의 "앞 코드 요약값(Key/Value)"을 준수 상태의 값으로
  치환**한 뒤 준수 선호 점수가 어떻게 바뀌는지 본다. (모델 구조상 이 값은 여러 계산 머리가
  공유하므로 **KV group 단위**로 정확히 치환.)
- **엄격한 공정성 장치(사전 등록):** ⓐ 준수 선호 점수 **값을 보고 표본을 고르지 않음**(순환 논증
  방지) — 토큰 수 일치 등 점수와 무관한 기준으로만 선별. ⓑ 후보 두 이름은 **모든 조건에서 동일한
  고정 문자열**을 채점. ⓒ 설계에 쓴 파일럿은 확증에서 제외(**홀드아웃**). ⓓ **무작위 donor 대조** —
  관계없는 camel 코드와 snake 코드를 각각 넣어 "내용이냐 스타일이냐"를 가른다.

**결과 (홀드아웃 40쌍 × 3 seed):**
- 기저 gap(준수−손상) = **+3.17**.
- **L25의 앞 코드값을 준수로 치환하니 Δ = +2.49** — 손상된 선호의 **약 79% 회복**(신뢰구간 0 배제).
- **특이성:** 관계없는 **camel** 코드를 넣어도 회복되지만(+2.47), **snake** 코드는 회복 안 됨(≈0).
  → 모델이 읽는 건 앞 코드의 **내용이 아니라 "camelCase냐 snake냐 하는 표기 스타일"**이다.
- **P2(보조 손잡이):** 모델이 지침을 보는 정도를 인위로 키우면 준수↑, 반으로 줄이면 손상보다도 더
  떨어짐 — 깔끔한 **용량-반응**(단조, ρ=+1.0).

**→ 주 가설 H3 확증:** L25의 코드 요약값이 함수명 표기 **선호**에 **인과적으로 기여**하며, 그 신호는
앞 코드의 내용이 아니라 **표기 스타일**이다.

---

## 5. 지금 진행 중 — 행동 재측정 (v2)

### 무엇을 / 왜 하나

3단계는 강한 인과 증거지만, 잰 것은 모델의 **내부 선호(확률)**이지 **실제로 써내는 함수 이름**이
아니다. 그래서 "선호가 바뀐다"를 넘어 **"실제 생성 준수율·후속 위반까지 바뀌는가"**를 확인해
내부 기제와 관측된 행동(1단계)을 잇으려 한다.

**1차 시도(greedy)는 천장에 걸렸다.** 항상 최고확률 이름 하나만 뽑으니, 지침이 워낙 강해 어느
조건이든 camelCase가 100% 나왔다 — 선호 마진(+3.17)은 움직여도 최종 출력이 안 뒤집힌 것. 그래서
정직한 현재 결론은:

> **L25의 코드 값이 표기 선호에 인과적으로 기여한다. 다만 현재 greedy 자유 생성 조건에서는
> 행동 변화가 관찰되지 않았다.**

### v2 방법론 (비포화 조건에서 다시)

1단계는 준수율이 30%까지 떨어지는 **비포화(깨지는) 조건**을 이미 갖고 있었다. 그 조건을 3단계로
가져와 행동을 재측정한다.

- **표집 생성:** 항상 1등만 뽑는 대신 확률대로 여러 번 뽑아(temperature 0.7) **준수율을 비율로**
  잰다. 그러면 "손상 70% → 개입 90%" 같은 변화가 드러난다.
- **작동점부터 보정(개입 없이):** 개입을 걸기 **전에**, 손상 준수율이 **20~80%로 떨어지는** 프롬프트
  설정을 먼저 찾는다. 게이트 3조건 — 손상 20~80% · (준수−손상)≥+20%p · 이름추출 성공률≥95%.
  **이 단계에서 개입 결과는 절대 보지 않는다**(개입이 잘 되는 프롬프트만 골라내면 "결과를
  만들어냈다"는 순환 논증이 되므로 구조적으로 차단).
- **엄격 비준수 지표:** 이름을 못 읽는 경우를 "위반 0"으로 세면 착시가 생기므로, **"제대로 된
  camelCase가 아니면 전부 비준수"**로 세고 분포를 다 공개한다.
- **작동점 통과 시에만** 새 홀드아웃에서 L25 개입/자기값(no-op)/무작위 camel·snake donor를 비교.
  주 검정: **P(camel | L25 준수 치환) − P(camel | 손상)** 의 신뢰구간이 0보다 크면, 개입이 선호뿐
  아니라 **실제 생성 준수율**까지 높인 것.

현재는 **보정 실험만 실행 가능**하게 안전장치를 걸어 두었고(본 개입은 게이트 미통과 시 자동 중단),
그 결과를 보고 다음 단계를 정한다.

---

## 6. 최종 논문 방향성

**확립된 기여 (이미 성립):**
1. **행동 수준(1단계):** 앞의 위반 코드가 준수율을 절벽처럼 낮추고, 첫 위반이 자기증폭한다.
2. **상관(2단계):** 그때 모델이 특정 층에서 앞 코드를 더 참조한다.
3. **기제 수준 인과(3단계):** 그 층(L25)의 코드값을 준수로 치환하면 준수 선호가 79% 회복되며,
   그 신호는 **표기 스타일**에 특이적이다. — **논문의 핵심 새로움.**

**정직한 경계:** 3단계의 인과는 현재 **내부 선호 수준**에서 확립되었다. greedy 자유 생성에서는
지침이 강해 행동이 포화되어 변화가 안 보였다(이 자체가 "선행 코드는 내부 선호를 바꾸지만, 인접·
강한 지침 아래서는 행동 문턱을 못 넘는다"는 정보). 또한 "context 길이 때문"이라는 단순 설명은
**배제된다** — 손상/준수 조건은 길이·위치가 동일한데도 선호 차이가 났기 때문.

**진행 중:** 1단계의 비포화 조건을 3단계로 가져와 **행동 수준까지 인과를 넓히는 재측정**. 성공하면
"선행 코드 표기 스타일 → 내부 선호 → 실제 준수 행동"이 하나의 이야기로 연결된다.

**남은 확장(후속):** 실제 오픈소스 코드에서의 재현(외적 타당도), 한국어 식별자 확장.
